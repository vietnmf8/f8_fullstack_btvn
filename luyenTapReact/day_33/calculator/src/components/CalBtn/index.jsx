export const CalcBtn = ({text, backgroundColor}) => {
    return (
        <div style={{backgroundColor: backgroundColor}}>
            <h2>Xin chao <span style={{color: 'red'}}>{text}</span></h2>
        </div>
    )
}